# torch được cài riêng trong Dockerfile (bản CPU-only từ PyTorch index) —
# không khai báo ở đây để tránh pip kéo nhầm bản CUDA từ PyPI.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
transformers>=4.46.0
sentencepiece>=0.2.0
protobuf>=4.25.0
